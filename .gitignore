# 런타임 작업 폴더(업로드/결과 임시파일)
.runtime/

# 파이썬 캐시
__pycache__/
*.pyc

# 로컬 실행 로그(서버 로그 — 저장소에 불필요)
*.log
streamlit*.log
st_*.log

# 데스크톱 exe 빌드 산출물(총 84MB — 소스만 저장소에 두고 결과물은 제외)
desktop/build/
desktop/dist/
desktop/dist_new/
# 로컬 자가진단 결과(내 PC 경로가 들어감)
desktop/진단결과.txt
