# 작업로그

SHP 좌표변환·병합·분할 도구 개발 기록. 상세 사양은 [README.md](README.md) 참고.

---

## 2026-07-29 (2) — 데스크톱판 체크박스도 X → ✓ (exe v1.1.0)

웹판을 고친 뒤 "데스크톱 exe도 고친 거냐"는 질문에 처음에 "데스크톱은 윈도우 기본
체크박스라 해당 없다"고 답했는데, **실제로 앱을 띄워 확인하니 데스크톱이 오히려 더 확실하게
X(☒)로 나오고 있었습니다.** 사용자가 지목한 "안전 변환 모드"·"면적 컬럼 추가" 바로 그 화면입니다.

- **원인**: `_build_style()`의 `style.theme_use("clam")`. clam은 Tk 내장 테마인데, 켜진
  체크박스의 indicator를 **✓가 아니라 X로 그립니다**(윈도우 기본 vista 테마였다면 ✓).
- **수정**: `_indicator_image()`로 네모칸 그림 4장(꺼짐/켜짐/비활성 꺼짐/비활성 켜짐)을
  픽셀 단위로 그려 `style.element_create("Check.indicator", "image", ...)`로 교체하고
  `TCheckbutton` 레이아웃을 갈아끼웁니다(`_apply_check_indicator`). 웹판이 CSS로 직접
  그리는 것과 같은 이유·같은 모양(파란 네모 + 흰 ✓). 실패하면 조용히 기본 모양 유지.
  - 테마를 vista로 바꾸지 않은 이유: 카드 배경·강조 버튼 등 앱 전체 배색이 clam 전제라
    테마를 통째로 바꾸면 외형이 무너집니다. indicator만 교체하는 쪽이 안전합니다.
  - PhotoImage는 런타임에 생성하므로 **exe에 별도 리소스 파일이 필요 없습니다**.
- **검증 함정**: `PrintWindow`(창 핸들 캡처)로 찍으면 image 기반 indicator가 실제와 다르게
  (꺼진 것도 체크로) 찍힙니다. **실화면 캡처(`CopyFromScreen`)로 봐야** 정확합니다.
  위젯 상태를 직접 찍어보면(`cb.state()`) 켜짐/꺼짐이 정상임을 확인할 수 있습니다.
- **재빌드**: v1.0.0 → **v1.1.0**(app.py APP_VERSION, build.bat VER, version_info.txt,
  사용설명서.txt). 결과물 `desktop/dist/SHP도구.exe`, `desktop/dist/SHP도구_배포_v1.1.0.zip`
  (dist/는 .gitignore라 저장소에는 안 올라갑니다 — 동료에겐 zip을 직접 전달).
- 참고: 오늘 웹판에서 한 나머지 개편(결과 세션 보존·ogrinfo 캐시)은 **Streamlit 구조 때문에
  생긴 문제**라 데스크톱엔 해당이 없습니다. 공간 처리 로직(core.py)은 07-28 이후 변경 없음.

---

## 2026-07-29 — 선택 UI를 체크박스로 통일 + 앱 반응속도 개편

사용자 지적 두 건에서 출발했습니다.
① "좌표계 변환할 때 고른 게 **파란색으로 변하기만** 하는데, 체크박스로 채워졌으면 좋겠다"
② "안전 변환 모드·면적 컬럼 추가 같은 **네모 체크박스가 눌러도 안에 ✓가 아니라 X처럼 보여서**
   선택이 안 된 줄 알겠다"

### ✅ A. 다중 선택 = 체크박스 목록 (`checkbox_picker`)

- `st.multiselect`는 고른 항목을 **색칠된 칩 + 지우기 `×`**로 보여줍니다. 칩이 "선택됨"인지
  "지우라는 버튼"인지 헷갈립니다. 4곳(변환할 레이어 / 병합할 레이어 / 분할할 SHP 레이어 /
  결합할 코드표 컬럼)을 전부 체크박스 목록으로 교체했습니다.
- `전체 선택` / `전체 해제` 버튼과 `선택됨 n / m개` 표시를 붙였습니다. 목록이 바뀌면
  (새로 업로드) 전체 선택 상태로 초기화합니다. 아무것도 안 고르면 실행 버튼이 비활성화됩니다.

### ✅ B. 체크 표시를 앱이 직접 그림 (`APP_CSS`)

- 로컬(Streamlit 1.58, 라이트/다크 양쪽)에서는 ✓로 보였습니다. 즉 **환경에 따라 달라지는**
  문제라, 기본 아이콘에 기대는 대신 CSS로 확정했습니다.
- 기본 아이콘(`background-image` SVG / 자식 SVG)을 지우고 **파란 네모 + 흰색 ✓**를
  `::after` 테두리 회전으로 직접 그립니다. 아이콘 폰트·SVG 로딩과 무관합니다.
- ⚠️ **Streamlit 버전마다 네모칸의 DOM 위치가 다릅니다**(실제 배포본과 로컬이 서로 달랐음).
  - (A) 로컬 1.58: `label > span(네모칸) + input + div(라벨)`
  - (B) Streamlit Cloud: `label > span(감춘 input 감싸개) + div(네모칸) + div(라벨)`
    — 이 span은 `clip-path: inset(50%)`로 **화면에서 잘려 있어** 여기에 색을 칠하면
    아무것도 안 보입니다. 처음 배포에서 실제로 이 함정에 걸려 클라우드에서는 스타일이
    안 먹은 것처럼 보였습니다(빨간 기본 체크박스 그대로).
  - 그래서 선택자를 두 벌로 씁니다: `span:first-child:not(:has(> input))`(A)와
    `span:has(> input) + div`(B). 배포본·로컬 양쪽에서 파란 네모+흰 체크를 눈으로 확인했습니다.
  - 참고: 클라우드 앱 페이지(`https://gis-shp-tool.streamlit.app`)는 **바깥 껍데기이고 앱은
    iframe(`/~/+/`) 안**에 있습니다. DOM을 직접 볼 때는 `/~/+/` 주소로 들어가야 합니다.

### ✅ C. 결과가 다운로드 누르면 사라지던 문제 (`save_result`/`render_result`)

- Streamlit은 위젯을 누를 때마다 스크립트를 처음부터 다시 실행하고, `if st.button(...)`은
  **누른 그 실행에서만** True입니다. 결과를 그 안에서 그리고 있었기 때문에 사용자가
  '결과 다운로드'를 누르는 순간(=재실행) 성공 메시지·통계표·경고·로그가 통째로 사라졌습니다.
- 실행 결과를 세션(`result__<슬롯>`)에 담고 버튼 블록 **밖에서** 매번 다시 그립니다.
  6개 실행 경로(변환/병합3종/분할/코드결합) 전부 적용. 다운로드 바이트는
  `_result_bytes`(cache_data, 최대 3건)로 캐시해 재렌더마다 zip을 새로 만들지 않습니다.

### ✅ D. 파일 조사 결과 캐시 — 클릭 한 번의 비용을 없앰

- `st.tabs`는 **4개 탭 내용을 전부 실행**합니다. 캐시가 없어서 체크박스 하나 누를 때마다
  탭마다 DBF를 다시 읽고 `ogrinfo`를 새 프로세스로 다시 띄웠습니다. 특히 '여러 레이어 병합'
  화면은 레이어당 `layer_epsg` + `layer_geom_family` = **레이어 수 × 2회**를 매번 실행합니다
  (레이어 10개면 클릭 한 번에 20회, 로컬 47ms/회 · 클라우드는 그 몇 배).
- `file_token(path)`=(경로, 수정시각ns, 크기)를 캐시 키로 삼는 `cached_probe`를 만들어
  모든 조사용 `ogrinfo` 호출(레이어 통계·EPSG·도형타입·필드명·GPKG 레이어명)과
  `read_dbf_preview`를 캐시했습니다. **파일이 바뀌면 토큰이 바뀌므로 새 결과 파일은 정확히
  다시 읽습니다**(캐시가 옛 값을 보여줄 위험 없음). `gdals()`도 10분 캐시.

### ✅ E. 기타 정리

- 레이어가 없을 때 `st.stop()` → `return`. `st.stop()`은 **스크립트 전체를 멈춰서** 첫 탭이
  비면 나머지 탭도 안 그려졌습니다.
- `sweep_old_workspaces()`: 브라우저를 그냥 닫으면 남는 `.runtime/shp_tool_*` 폴더를
  세션 시작 때 한 번, 12시간 지난 것만 정리(클라우드 디스크 누수 방지).
- `requirements.txt`의 streamlit 하한을 실제 사용 API에 맞춰 `>=1.46`으로 정정
  (`st.dataframe(width="stretch")`가 필요).

### 검증

로컬 Streamlit(포트 8512)에 실제 SHP 2개(UQ111 국토계획 도시지역 득산동·배미동) zip을 올려
브라우저로 6개 실행 경로를 전부 눌러 확인했습니다. 좌표계 변환(+면적) 11/13 피처 손실 0,
extent가 5186 범위로 정상 변환 / 여러 레이어 병합 24→24 / 속성만 합치기 area_m2·area_sum 추가 /
분할 22개 생성 / 코드 결합 매칭률 64% 및 결과 미리보기. 결과 표시 후 다른 체크박스를 눌러
재실행시켜도 결과 블록이 그대로 남는 것까지 확인했습니다.

---

## 2026-07-28 — 면적 소수점 버그 + 병합 누락 4종 수정

사용자 제보 두 건("면적 소수점 2자리로 설정했는데 소수점이 많이 나온다",
"여러 SHP를 MERGE했는데 다 안 합쳐지는 것 같다")에서 출발해, 로컬 GDAL 3.7로
**모든 증상을 재현한 뒤** 원인을 확정하고 고쳤습니다.

### ✅ A. 면적 소수점 자리수가 무시됨 (app.py / desktop/core.py)

- **증상**: 소수점 2자리로 지정해도 결과 SHP에 `11027.299999999999272`처럼 15자리가 나옴.
- **재현**: 앱과 동일한 명령으로 확인 → 결과 필드 정의가 `area_m2: Real (24.15)`.
- **원인**: `ROUND()`는 정상 동작함(값은 double이라 2진수로 `11027.30`이 정확히 표현
  안 될 뿐, 이건 정상). 진짜 원인은 **저장 단계**. SQL로 계산해 만든 컬럼은 "소수점 몇
  자리 필드인지" 정보가 없어서 GDAL Shapefile 드라이버가 **기본값 폭 24 / 소수점 15**로
  필드를 만들고, DBF는 값을 *글자*로 저장하므로 15자리가 그대로 파일에 박힘.
  → 즉 사용자가 고른 자리수는 계산에만 반영되고 저장 형식엔 전혀 반영되지 않았음.
- **수정**: `decimal_cast_args()` 신설. 최종 SHP 저장 때
  `-dialect OGRSQL -sql "SELECT ..., CAST(area_m2 AS numeric(w,d)) AS area_m2, ... FROM 레이어"`
  로 **필드 정의 자체**를 바꿈. `gpkg_to_final`/`sqlite_sql_to_output`에 `decimal_fields`
  인자를 추가하고 `add_area_column`·`aggregate_keep_geometry`가 넘겨줌.
- **구현 중 확인한 함정 2가지**:
  1. `-dialect OGRSQL`을 **반드시 명시**해야 함. GPKG를 소스로 `-sql`을 주면 GDAL이 GPKG
     내장 SQLite로 넘겨버려 `CAST(... AS numeric(w,d))`의 자리수 지정이 무시됨(`Real (0.0)`).
  2. OGRSQL은 `SELECT *, 식` 형태를 지원하지 않음 → 컬럼을 전부 나열해야 함
     (`layer_field_names()`로 ogrinfo에서 실제 컬럼 목록을 읽어옴).
- **검증**: `area_m2: Real (22.2)` / 값 `11027.30`, `2592.54`. 0자리 지정 시
  `Integer64 (18.0)` / `2500`(정수 필드로 떨어지는 게 자연스러움). 한글 컬럼명 혼재도 정상.
- **GPKG 출력은 원래 문제없음**(소수점 자리 개념이 없어 값 그대로 저장 → QGIS가 `11027.3` 표시).

### ✅ B. GPKG 입력 시 새 컬럼명 중복 체크 누락

- `aggregate_keep_geometry`가 `columns_for_layer()`로 기존 컬럼을 확인했는데, 이 함수는
  **DBF만 읽어** GPKG 입력이면 빈 목록을 반환 → 이미 `area_m2`가 있어도 못 걸러냄.
- 수정: ①단계에서 만든 GPKG의 실제 컬럼(`layer_field_names(work, "src")`) 기준으로 확인.

### ✅ C. 병합 시 재투영이 안 됨 — **가장 조용히 틀리던 버그**

- **재현**: 4326 레이어를 5186 레이어에 `-append` → `exit=0`, 경고 없음, feature 개수 정상.
  그런데 도형 좌표가 `127.1 35.8` 그대로 들어감(extent가 `(127.1, 0) - (9050, 50)`).
  QGIS에서 내 데이터로 줌하면 그 레이어만 화면 밖 → **"안 합쳐졌다"로 보임**.
- **원인**: `-append`는 재투영을 하지 않음. `병합 전 목표 EPSG 통일` 칸이 기본 빈칸이라
  `-t_srs`가 아예 안 붙음.
- **수정**: `merge_layers`가 `layer_epsg()`로 레이어별 EPSG를 읽어, 목표가 비어 있고
  좌표계가 서로 다르면 **첫 레이어 좌표계로 자동 통일**(+로그). `.prj` 없는 레이어는 경고.
  병합 실행 전 UI에 레이어별 좌표계·도형 타입 표와 경고를 표시.
- **검증**: 자동 통일 후 extent가 5186 범위로 정상 변환됨.

### ✅ D. 같은 파일명 업로드 시 덮어쓰기

- `save_uploads`가 `input_dir / uploaded.name`으로 **평평하게** 저장 → 시군구별로 받은
  `연속지적도.shp`처럼 이름이 같으면 나중 파일이 앞 파일을 조용히 덮어써서 목록에 안 나타남.
- **수정**: 같은 이름이 다시 오면 `중복2/`, `중복3/` 하위 폴더로 분리(확장자별 등장 순번을
  세어 `.shp/.shx/.dbf/.prj`가 **같은 폴더에 모이도록** 함 — SHP은 sidecar가 흩어지면 깨짐).
  `save_uploads`가 `(입력폴더, 중복목록)`을 반환하고 사이드바에 경고 표시.
  `discover_layers`는 이름이 겹치면 폴더명을 앞에 붙여 구분(`중복2/도로`).
- **검증**: 같은 이름 SHP 두 세트 업로드 → 2개 다 살아남고 이름이 구분됨(예전엔 1개만 남음).
- ZIP 업로드는 원래부터 각각 폴더가 생겨 안전함(가장 확실한 방법).

### ✅ E. 도형 타입 혼합 시 피처 소실 (순서 의존)

- **재현**: 면 9개 + 점 1개 → GPKG 중간 단계는 10개 정상인데, 최종 SHP 저장에서
  `ERROR 1: Attempt to write non-polygon (MULTIPOINT) geometry to POLYGON type shapefile` →
  9개만 남음. **점이 첫 레이어면 반대로 폴리곤 9개가 전멸하고 1개만 남음.**
- **원인**: SHP은 한 파일에 한 가지 도형 타입만 담음. GPKG 레이어가 첫 레이어 타입으로
  고정(`-nlt PROMOTE_TO_MULTI`)되는 것도 순서 의존의 원인.
- **수정**: `layer_geom_family()`로 점/선/면을 미리 판정. 섞였으면 `-nlt GEOMETRY`로 GPKG
  레이어를 만들고, SHP을 골랐어도 **GPKG로 저장**(안내 로그 + UI 경고).
- **검증**: 어떤 순서로 넣어도 10개 전부 보존. 섞이지 않은 정상 케이스는 기존대로 SHP 9개.

### ✅ F. ERROR/Warning이 접힌 로그 안에만 있어 안 보임

- GDAL은 '타입이 안 맞아 0으로 넣었다'(`Value '가나다' ... parsed incompletely to integer 0`)
  같은 **데이터가 조용히 망가지는 상황**을 대부분 Warning으로만 알리고 성공(exit 0)으로 끝냄.
- **수정**: `log_problems()` + `render_log_problems()`로 결과 바로 아래에 요약 표시
  (변환·병합·분할·코드결합 공통). GDAL 설치 잡음(`tms_NZTM2000`, `GDAL_DATA`)은 제외.

### 신설 공통 헬퍼 (app.py / desktop/core.py 양쪽 동일)

| 함수 | 하는 일 |
|---|---|
| `layer_field_names(path, layer)` | ogrinfo로 실제 속성 컬럼명 목록(SHP·GPKG 공통) |
| `layer_epsg(path, sublayer)` | 좌표계 EPSG 코드(WKT의 **마지막** EPSG가 CRS 코드) |
| `layer_geom_family(path, sublayer)` | 도형 타입을 점/선/면/기타로 정규화 |
| `decimal_cast_args(...)` | SHP 소수점 자리수 고정용 OGRSQL CAST 인자 생성 |
| `log_problems(log)` | 로그에서 ERROR/Warning 줄만 추출 |

> ⚠️ **구현 중 놓쳤다가 잡은 것**: `ogrinfo -so <파일>`은 레이어 **목록만** 출력합니다.
> 필드/좌표계/도형타입을 읽으려면 `-so -al`이거나 레이어명을 함께 줘야 합니다.
> 처음에 `-al` 없이 짜서 C·E 감지가 전부 무동작이었고, e2e 테스트로 발견했습니다.

### 면적 산정 원리(사용자 질문) — 정리해 README에 반영

`ST_Area`는 평면 신발끈 면적이라 **알고리즘 오차는 0**이고, 정확도는 좌표계가 좌우합니다.
실측(GDAL 3.7): 1,000,000㎡ 땅 → 5186 = 1,000,000 / **5179(UTM-K) = 999,249(−0.075%)** /
5174 = 1,000,013 / 4326 = 제곱도(무의미). 연속지적도가 대부분 5179이므로 면적은 5186으로
통일해 계산할 것. 다만 **가장 큰 오차는 원본 도면**(연속지적도 ↔ 토지대장 법정면적, 필지당
수 % 차이)이라 소수점 자리수를 늘려도 의미 없음. 상세 표는 README "면적 산정 원리와 정확도".

### 검증 방식

streamlit을 스텁으로 대체하고 `app.py`/`desktop/core.py`의 처리 함수를 직접 호출하는
e2e 스크립트로 A~F 전부 + 정상 케이스 회귀(같은 스키마·같은 좌표계 3개 → SHP 9개)를 확인.
실제 사용자 데이터(전주 정비 SHP 등)로 눈검수는 아직 안 함.

---

## 📌 현재 상태 / 다음에 이어서 할 것 (2026-07-06 기준)

**배포**
- GitHub: `uurimii16/gis-shp-tool` (이 폴더가 clone, `origin/main` 연결됨).
- 공개 웹앱: https://gis-shp-tool.streamlit.app (Streamlit Community Cloud). 클라우드는 apt `gdal-bin`(`packages.txt`).
- 절전방지: `.github/workflows/keep-awake.yml`(6h마다 방문·깨우기) 이미 배포됨.
- 로컬 실행: `cd` 후 `python -m streamlit run app.py` (QGIS 3.30.3 내장 GDAL 3.7 자동탐색).

**오늘(2026-07-06) 반영된 것 (아래 상세 참고)**
1. dissolve 도형 깨짐 수정 — `dissolve_one_layer`를 "복구(-makevalid)→ST_Union" 2단계로.
2. GPKG 결과 + 면적 실패 수정 — `resolve_layer_and_geom`로 실제 레이어명·geom컬럼 탐색.
3. **신규 모드 "속성만 합치기(도형·개수 유지)"** — 도형 안 바꾸고 그룹별 값/면적 부착.
   - 면적: 각 폴리곤 `area_m2` + 그룹 실면적 `area_sum`(=`ST_Area(ST_Union(geometry))`, 겹침 1번만 계산 → 정확).
   - 함수 `aggregate_keep_geometry`, 컬럼명 ASCII(SHP 필드명 제한 회피).

**⚠️ 다음 세션에서 꼭 할 것**
- **사용자 실제 데이터(전주 정비/용도지역 SHP)로 최종 검수**: ① dissolve+면적, ② 속성만 합치기+area_sum 둘 다. 특히 원본 `.prj`(좌표계) 유무와 5186 변환 결과 ㎡ 정상 여부.
- **클라우드에서 동작 확인**: `-makevalid`(CLI)·`ST_Union`·`GEOMETRY_NAME` 이 apt gdal-bin에서도 되는지(로컬 GDAL 3.7만 검증됨). ST_MakeValid SQL 함수는 클라우드 미지원 가능성 있어 일부러 회피함.
- 코드 결합 탭 중복키 이슈(코드표 CSV에 같은 키 여러 줄 → fid UNIQUE 충돌로 실패) 방어 안 됨 — 필요시 dedupe/경고 추가.

---

## 2026-07-03

기존 3탭(좌표변환·병합·분할) 기반 앱에 아래 기능을 추가·검증했습니다. 모든 항목은 로컬 QGIS 내장 GDAL 3.7로 실데이터 end-to-end 검증 완료.

### 신규 기능
- **변환 전후 feature count / extent 비교** — `ogrinfo` 출력을 파싱해 변환 전후 피처 수와 범위를 표로 비교.
- **도형 유효화 `-makevalid` 옵션** — 변환·병합·분할 공통. self-intersection 등 불량 도형 자동 보정.
- **병합(dissolve) 속성 집계 옵션** — 기준 컬럼 외 속성을 SUM/AVG/MAX/MIN/COUNT로 요약(컬럼별 지정).
- **작업 로그 다운로드** — 각 탭 처리 로그를 텍스트로 저장.
- **GDAL 미설치 안내 강화** — 설치/연결 방법을 사이드바에 단계별로 안내.
- **GPKG 내부 복수 레이어 선택** — `ogrinfo`로 GPKG 내부 레이어를 나열해 `파일명:레이어명` 단위로 개별 인식·선택.
- **면적 컬럼(area_m2, ㎡) 추가** — 변환·병합 탭. `ST_Area` 기반, 투영 후 결과에 후처리로 계산(미터 좌표계에서 정확). 경위도(4326)는 경고.
- **4. 코드 결합 탭(신규)** — SHP 속성의 MNUM 등에서 `substr(컬럼, 시작, 길이)`(기본 21,6)로 코드를 뽑아 용도지역 코드표 CSV를 LEFT JOIN. 원본 DBF를 직접 편집하지 않고 결합된 새 SHP/GPKG 생성.
  - 코드표 CSV 인코딩 후보별 깨짐 점수 표시 + 수동 선택(UTF-8-SIG/CP949/EUC-KR/UTF-8).
  - 대상 SHP 인코딩 확인, MNUM 컬럼/시작·길이 지정, 추출 코드·매칭률 미리보기.
- **안전 변환 모드(변환 탭, 기본 ON)** — 불안정 SHP를 다른 좌표계로 재투영할 때 폴리곤이 한두 개 사라지는 문제 방지.
  - ① 원본 좌표계 확정(`-a_srs`) + `makevalid`로 네이티브 좌표에서 도형 복구 → ② 재투영(`-t_srs`) + `makevalid` 2단계.
  - 입력 → 복구후 → 출력 단계별 피처 수 리포트 + 손실 발생 시 빨간 경고.

### 유지보수
- `use_container_width`(폐기 예정) → `width="stretch"` 전면 교체.
- 임시 작업 폴더를 시스템 Temp → 앱 폴더 내 `.runtime/`로 변경(권한 오류 회피).

### 클라우드 배포 후 수정
- **한글 손상(???) 버그 수정** — SQLite dialect로 SHP를 직접 쓰면 일부 GDAL 버전(클라우드 apt gdal-bin)에서 `-lco ENCODING`이 무시돼 한글이 ISO-8859-1로 손상됨. 공용 헬퍼 `sqlite_sql_to_output`으로 공간 SQL 결과를 UTF-8 GPKG로 만든 뒤 SHP로 재변환하도록 우회(면적/dissolve/코드결합 공통 적용). dissolve에 누락됐던 출력 인코딩도 지정.
- **면적 소수점 자리수 선택** — 변환·병합 탭에서 area_m2 소수점 0~6자리 지정.
- **결과물 빈손 방지 안전망** — SHP 저장이 실패하면 자동으로 GPKG로 대체 저장(`gpkg_to_final`). 변환·병합·분할 아님(분할 제외)·면적·코드결합 전 경로에 적용. 대체 발생 시 화면에 안내. 모든 처리 함수가 실제 저장 경로를 반환하도록 통일, 다운로드는 `download_for_path`가 실제 형식에 맞춰 제공.

### 배포 준비
- `packages.txt` 추가(`gdal-bin`, `libsqlite3-mod-spatialite`) — Streamlit Community Cloud에서 GDAL/spatialite 자동 설치용.
- `.gitignore` 추가 — `.runtime/`, `__pycache__/`, 런타임 로그 제외.

---

## 2026-07-06

### ✅ 버그수정: GPKG 결과 + 면적(area_m2) 추가 실패
- **증상**: dissolve(또는 변환) 결과를 **GPKG로 저장 + 면적 컬럼 추가** 시 면적 단계 실패.
- **원인**: `add_area_column`이 레이어명을 **파일명(stem)** 으로 가정했으나, GPKG는 내부 레이어명이 다름(예: dissolve 결과는 `result`). SHP는 레이어명=파일명이라 우연히 동작했음.
- **수정**: `resolve_layer_and_geom(path)` 헬퍼 추가 — GPKG는 `ogrinfo`로 실제 레이어명·지오메트리 컬럼명을 읽고, SHP는 (stem, 'geometry') 사용. `add_area_column`이 이를 써서 `FROM <실제레이어> ST_Area(<실제geom컬럼>)` 생성. 변환 탭의 GPKG+면적 견고성도 함께 향상.
- **검증**: 흩어진 자연녹지 3조각 + 주거 1조각 → dissolve → 면적. GPKG/SHP 둘 다 `면적ok=True`, 자연녹지 area_m2=300(3조각 합산), 주거=100. (흩어진 폴리곤도 하나의 MultiPolygon으로 묶여 면적 합산됨을 확인)

### 신규 기능: "속성만 합치기(도형·개수 유지)" 모드 (레이어 병합 탭)
- dissolve와 별개로, **도형을 전혀 안 바꾸고** 그룹 기준 컬럼별 값을 각 피처에 새 컬럼으로 붙이는 모드. 함수 `aggregate_keep_geometry`(자기조인: 그룹 집계 서브쿼리와 기준 컬럼 1:1 LEFT JOIN → 개수·fid 안전).
- **면적 옵션(기본 ON)**: 원본에 면적 열이 없어도 도형에서 계산. 각 폴리곤 `area_m2`(ST_Area) + 그룹별 `area_sum`. **area_sum은 `ST_Area(ST_Union(geometry))`로 '실제 합친 면적'을 구해 폴리곤이 겹쳐도 겹친 부분을 한 번만 셈**(단순 SUM은 겹침 중복→과대평가). union 안전 위해 ①단계에서 CLI `-makevalid`로 도형 복구(유효 도형은 불변, 클라우드 gdal-bin 호환 위해 ST_MakeValid SQL 함수 대신 CLI 사용). 면적 정확도 위해 ①단계에서 재투영(target_epsg).
- 컬럼명은 ASCII(`area_m2`/`area_sum`)로 — SHP 10바이트 필드명 제한에서 한글이 잘려 값이 깨지는 것 방지(검증: 한글명 `area_m2_합계`는 SHP에서 0으로 깨짐 확인 → ASCII로 수정).
- 속성 집계(SUM/AVG/MAX/MIN/COUNT/GROUP_CONCAT)도 선택 가능(면적과 별개, 선택).
- 검증(로컬 GDAL 3.7): 겹치는 자연녹지 2개→2개 유지 area_sum=175(정확), 흩어진 자연녹지 3개→3개 유지 area_sum=300. SHP/GPKG 둘 다 정상.

### ✅ 해결: dissolve 결과 폴리곤 도형 깨짐 (아래 🔴 이슈 종결)
- **수정**: `dissolve_one_layer`를 "복구 먼저 → union"의 **2단계**로 재작성(합의된 방향 3안 채택).
  1. **① 도형 복구**: 원본을 `ogr2ogr -makevalid`로 UTF-8 GPKG(`-nln src`, `-lco GEOMETRY_NAME=geometry`)로 복구. 재투영 없이 네이티브 좌표에서. spatialite `ST_MakeValid` SQL 함수에 의존하지 않고 GDAL의 `-makevalid`(GEOS MakeValid)를 사용해 클라우드 호환성 확보.
  2. **② union**: 복구된 GPKG에서 `ST_Union(geometry) ... GROUP BY` 실행 + 최종 `-makevalid`(항상) + 선택적 `-t_srs` 재투영. 이후 `sqlite_sql_to_output` 경로로 인코딩 안전 저장.
- **복구는 항상 수행**(makevalid 체크박스와 무관). 이것이 근본 해결이므로 기본 OFF로 두면 안 됨.
- **주의점(구현 중 발견)**: GPKG 지오메트리 컬럼 기본명은 `geom`이라 union SQL의 `FROM src`에서 `geometry`를 못 찾음 → 복구 단계에 `-lco GEOMETRY_NAME=geometry`로 컬럼명을 고정해 해결.
- **검증(로컬 QGIS GDAL 3.7, end-to-end)**: 위상 오류(자기교차 bowtie + 겹침) 폴리곤 테스트셋으로 비교.
  - 수정 전: 불량 그룹이 `ST_IsValid=-1`(GEOS TopologyException), 면적 null → 깨짐 재현.
  - 수정 후: 모든 그룹 `ST_IsValid=1`. 인접 정상 사각형 2개는 **외곽선 그대로 20×10 단일 폴리곤으로 병합**(링 1개, 내부 공유 경계만 제거, 바깥 윤곽 보존 확인). 속성만 합쳐지고 도형 모양은 불변.
- **UI**: 병합 탭 caption에 "바깥 윤곽 유지·내부 공유 경계만 제거·복구 자동 수행" 안내 추가.
- **남은 확인(배포 시)**: 클라우드 gdal-bin에서도 `-makevalid`/`GEOMETRY_NAME` 동작 확인. 사용자 실제 데이터(전주 정비 SHP)로 최종 눈검수 권장.

---

## 🔴 미해결 / 다음 작업 (2026-07-03 종료 시점) — ✅ 위 2026-07-06에서 해결됨

### 이슈: dissolve 결과 폴리곤 선형(geometry)이 깨짐
- **증상**: "한 SHP 내 컬럼값 기준 병합(dissolve)" 실행 시 텍스트(속성)는 정상인데 **폴리곤 경계선이 이상하게 깨짐**(스파이크/잘못된 선).
- **원인 확정**: 결과를 **GPKG로 출력해도 동일하게 깨짐** → GPKG→SHP 변환 문제가 **아님**. **dissolve의 `ST_Union` 연산 자체**가 원인. GPKG→SHP는 정점 좌표를 그대로 복사하므로 도형 왜곡 없음(곡선만 SHP 미지원인데 dissolve 결과는 직선이라 무관).
- **추정 세부원인**: 원본 폴리곤의 위상 오류(자기교차/겹침/미세 슬리버/링 방향)를 **복구하지 않고** `ST_Union`하면 GEOS가 깨진 결과를 냄. 현재 `dissolve_one_layer`는 `-makevalid`를 union **다음**에 적용(순서 비효율). makevalid 체크박스 기본 OFF라 대개 복구 없이 union됨.

### 다음에 할 수정 (합의된 방향)
1. **dissolve를 "복구 먼저 → union" 순서로**: SQL을 `ST_Union(ST_MakeValid(geometry))` 형태로 바꿔 각 도형을 union 전에 복구. (spatialite `ST_MakeValid` 지원 여부 확인 필요 — 로컬 GDAL 3.7 및 클라우드 gdal-bin/spatialite 둘 다 테스트할 것)
2. 그래도 남는 오류 대비 **결과에 최종 makevalid 한 번 더**.
3. 대안/보강: dissolve도 안전변환처럼 **2단계**(① 원본 makevalid로 GPKG 생성 → ② 그 GPKG에서 `ST_Union GROUP BY`)로 만들면 spatialite `ST_MakeValid` 의존을 피할 수 있음. 이 방식이 더 견고할 수 있으니 우선 검토.
4. 관련 함수: `dissolve_one_layer`(app.py). 좌표변환 탭의 `convert_layer_safe`가 참고 패턴(2단계 makevalid).

### 재현/검증 방법
- 로컬: `C:\Users\PC\...\scratchpad`에 만든 한글 속성 폴리곤 SHP로 dissolve → 결과 도형 확인. (단, 실제 깨짐은 위상 오류 있는 원본에서 잘 재현되므로, 자기교차/겹침 있는 테스트 폴리곤을 만들어야 함)
- 사용자 실제 데이터: 전주 정비 관련 SHP로 추정. 다음 세션에 원본을 QGIS에서 열어 원본 자체 위상 상태부터 확인 권장.

### 참고: 지금까지 배포 상태(정상)
- 인코딩(???) 수정·면적 소수점·빈손방지 대체는 배포 완료(커밋 f7899ef). 텍스트/한글은 정상. dissolve **도형 품질만** 미해결.
